# The cost model, registry, and scenario map (kvlab.memory / registry / scenarios)
# run on the Python standard library alone. Everything below is for the model
# backends and evaluations.

# Upper bounds are deliberate: the harness touches transformers cache internals
# (model.py has branches for the 4.x and 5.x Cache APIs), so untested majors are
# excluded. Tested against torch 2.9 / transformers 5.14; result JSONs record
# the exact versions of every run.
torch>=2.1,<3
transformers>=4.44,<6
datasets>=2.19,<5

# Optional: real long-context methods and standardized evaluation.
# kvpress>=0.2
# optimum-quanto>=0.2   # for quantized KV cache
